import { IndustrialWorkforceShell } from "@/components/workforce/industrial-workforce-shell";

export default function WorkforceLayout({ children }: { children: React.ReactNode }) {
  return <IndustrialWorkforceShell>{children}</IndustrialWorkforceShell>;
}
